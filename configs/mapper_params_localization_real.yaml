slam_toolbox:
  ros__parameters:
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ROS Parameters
    odom_frame: robot_odom
    map_frame: robot_map
    base_frame: robot_base_footprint
    # Chassis self-hits removed by scan_footprint_filter (started in the
    # entrypoint); min_laser_range below only cuts the near arc by distance.
    scan_topic: /robot/front_laser/scan_filtered
    mode: localization # mapping

    # Loads the map built during the 2026-09-16 real-robot mapping session
    map_file_name: /fnh_pkgs/src/renee_simulation/maps/real_robot_environment_v4
    # slam_toolbox starts localizing from the map's origin (wherever
    # mapping began) — correct only if the robot happens to be
    # there when this launches. If it's elsewhere, use RViz's "2D Pose
    # Estimate" tool to nudge it once running
    debug_logging: false
    throttle_scans: 1
    # Must be nonzero: in slam_toolbox 2.8.3 the periodic publishTransformLoop
    # is the only place robot_map->robot_odom is broadcast, and it returns
    # immediately at 0.0, so nothing is published (localize_real's
    # healthcheck and navigation-real then hang, and /initialpose has no
    # visible effect). The earlier "flip-flop" that led to 0.0 was a second
    # publisher: the Vogui's onboard AMCL robot_map->robot_odom edge,
    # which vogui_ros1_ros2_bridge's tf_map_filter.py now drops.
    transform_publish_period: 0.05
    map_update_interval: 5.0
    resolution: 0.05
    # Above the front laser's ~0.29 m self-hit arc on the robot body: in
    # localization mode slam_toolbox rasterizes live scans into /map, so
    # self-hits became lethal cells under the robot and navfn (global_costmap
    # static_layer on /map) could not plan out of its own footprint.
    min_laser_range: 0.35
    max_laser_range: 10.0 #for rastering images
    minimum_time_interval: 0.5
    transform_timeout: 0.2
    tf_buffer_duration: 30.
    stack_size_to_use: 40000000 #// program needs a larger stack size to serialize large maps
    # See configs/mapper_params_online_async.yaml: not used by this
    # (automated) pipeline, and was implicated in the same dual-publisher
    # /tf issue above.
    enable_interactive_mode: false

    # General Parameters
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.5
    minimum_travel_heading: 0.5
    scan_buffer_size: 3
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    do_loop_closing: true
    loop_match_minimum_chain_size: 3
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Correlation Parameters - Correlation Parameters
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Correlation Parameters - Loop Closure Parameters
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03
    loop_search_maximum_distance: 3.0

    # Scan Matcher Parameters
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0

    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
    min_pass_through: 2
    occupancy_threshold: 0.1
